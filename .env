VITE_BACKEND_API_HOST=https://awd-ia03-be.onrender.com
# Copy this file to `.env.local` or `.env` and change the host if your API runs elsewhere.
# Vite exposes variables prefixed with VITE_ to the client as import.meta.env.VITE_...
